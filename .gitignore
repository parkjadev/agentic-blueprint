# IP / internal docs
Agentic-Blueprint-v2.0.md

# Subagent isolation worktrees (created by Agent tool with isolation: 'worktree')
.claude/worktrees/

# IDE
.idea/
*.iml
.vscode/

# Environment (local overrides — never commit)
.env.local
.env.*.local
