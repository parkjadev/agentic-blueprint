# Signal — dependency health

Weekly output of the `dependency-audit` scheduled task. Latest scan results + open advisories + upgrade suggestions.

**Format:** one section per scan run. Sorted newest-first.

---

*No scans yet. The dependency-audit scheduled task runs Monday 06:00 UTC once /signal init has wired it.*
