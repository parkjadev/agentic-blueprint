# Signal — API spend

Monthly Anthropic API spend rollups. Populated by `/signal status` when `ANTHROPIC_ADMIN_KEY` is configured (opt-in).

**Format:** one row per calendar month. Includes month-to-date total and delta vs previous month.

---

*No spend data yet. Configure `ANTHROPIC_ADMIN_KEY` in your scheduled-task environment and run `/signal status` to start capturing.*
